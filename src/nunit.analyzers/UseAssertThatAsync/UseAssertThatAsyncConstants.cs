namespace NUnit.Analyzers.UseAssertThatAsync;

internal static class UseAssertThatAsyncConstants
{
    internal const string Title = "Use Assert.ThatAsync";
    internal const string Message = "Replace Assert.That with Assert.ThatAsync"; // TODO:
    internal const string Description = "Use Assert.ThatAsync to modernize and simplify testing asynchronous code."; // TODO:
}
