namespace NUnit.Analyzers.UseAssertThatAsync;

internal static class UseAssertThatAsyncConstants
{
    internal const string Title = "Use Assert.ThatAsync";
    internal const string Message = "Replace Assert.That with Assert.ThatAsync";
    internal const string Description = "You can use Assert.ThatAsync to assert asynchronously.";
}
