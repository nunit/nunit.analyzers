namespace NUnit.Analyzers.Constants
{
    internal static class SomeItemsConstraintUsageConstants
    {
        internal const string Title = "Use SomeItemsConstraint for better assertion messages in case of failure.";
        internal const string Message = "Use {0} constraint instead of direct comparison for better assertion messages in case of failure.";
        internal const string Description = "Using SomeItemsConstraint will lead to better assertion messages in case of failure.";
    }
}
