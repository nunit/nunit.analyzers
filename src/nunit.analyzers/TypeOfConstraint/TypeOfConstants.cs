namespace NUnit.Analyzers.TypeOfConstraint
{
    internal static class TypeOfConstants
    {
        public const string Title = "Consider using Is.TypeOf constraint instead of performing equality check between types";
        public const string Message = "Consider using Is.TypeOf constraint instead of performing equality check between types for better assertion messages";
        public const string Description = "Consider using Is.TypeOf constraint instead of performing equality check between types for better assertion messages.";
    }
}
